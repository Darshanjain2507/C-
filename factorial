#include<iostream.h>
#include<stdio.h>
int main()
{
    int j,n,x;
    cout<<"enter the number:----->";
    cin>>n;
    x=n;
    for(j=n-1;j>=1;j--)
    {     x=x*j;      }
         cout<<x;

    return 0;
}
