#include<stdio.h>
int main()
{
    int j,n,x;
    printf("enter the number:----->");
    scanf("\n%d",&n);
    x=n;
    for(j=n-1;j>=1;j--)
    {     x=x*j;      }
          printf("%d",x);

    return 0;
}
